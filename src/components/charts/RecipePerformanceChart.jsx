import { Box } from "@mui/material";

/**
 * @returns {JSX.Element|null}
 * @constructor
 *
 * @todo implement chart and dynamically query data
 */
function RecipePerformanceChart() {
  return (
    <Box
      sx={{
        background: "grey",
        height: "200px",
        width: "100%",
        color: "white",
        padding: 10,
      }}
    >
      CHART
    </Box>
  );
}

export default RecipePerformanceChart;
